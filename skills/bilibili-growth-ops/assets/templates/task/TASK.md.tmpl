# {{taskTitle}}

## 任务卡片

- 任务 ID：`{{taskId}}`
- 产品：`{{productSlug}}`
- 策略：`{{strategySlug}}`
- 模式：`{{mode}}`
- 状态：`{{status}}`
- 创建时间：`{{createdAt}}`

## 任务目标

{{goal}}

## 使用说明

- 这个文件是任务主控壳，不是完整业务执行细则。
- 具体步骤、批次说明和补充工作文件，应由 agent 按当前任务需要在任务目录中继续生成。
- `Strategy` 提供打法主线，这个任务文件负责承接这一次产品运行的当前执行状态。
- 主 Agent 负责维护这份文件，并让它始终与当前阶段保持一致。
- 副 Agent 应根据派工单执行，并把结果交回主 Agent 做统一回写，而不是直接改这个文件。

## 阶段清单

{{stageChecklist}}

## 审核规则

- 新阶段的首轮外发必须先审核。
- 主 Agent 负责维护 `TASK.md` 和 `WORKLOG.md`。
- 副 Agent 可以执行被派发的阶段片段，但必须把结果交回主 Agent 做回写。

## 当前焦点

- 当前阶段：`{{currentStageId}}`
- 当前说明：{{currentStageNote}}

## 工作规则

- 使用 `WORKLOG.md` 记录简短的时间线更新。
- 使用 `outputs/<stage-id>/` 存放结果文件和阶段产物。
- 只有当额外任务说明确实能提升当前执行效率或清晰度时，才创建更多补充文件。

## 恢复说明

- 恢复任务时，优先同时参考 `task-state.json`、`WORKLOG.md` 和关键动作记录 `OperationRecord`。
